# Deployment for the "api-add-game" service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-add-game
  labels:
    app: api-add-game
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-add-game
  template:
    metadata:
      labels:
        app: api-add-game
    spec:
      containers:
      - name: api-add-game
        env:
          - name: PYTHONPATH
            value: "/Backup:/utils"
          - name: DB_HOST
            value: "data-base-service"
          - name: DB_DATABASE
            value: "Gabo_base"
          - name: DB_USER
            valueFrom:
              secretKeyRef:
                name: db-credentials
                key: DB_USER
          - name: DB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: db-credentials
                key: DB_PASSWORD
          - name: DB_PORT
            value: "3306"
          - name: BACKUP_PATH
            value: "/Backup/back_up"
          - name: FLASK_APP_URL
            value: "http://app-service:8000/ready"
          - name: API_AP_URL
            value: "http://api-add-player-service:8010/ready"
          - name: API_AG_URL
            value: "http://api-add-game-service:8020/ready"
          - name: API_AG_URL_INDEX
            value: "http://api-add-game-service:8020/reload_indexbyname"
        image: lagadglav/gabo-api-add_game:latest
        ports:
          - containerPort: 8020
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"


